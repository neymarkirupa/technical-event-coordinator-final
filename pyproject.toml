[project]
name = "technical-event-coordinator"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "requests",
    "openai",
    "openenv>=0.1.13"
]

[project.scripts]
server = "server.app:app"