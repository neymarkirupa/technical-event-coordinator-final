[project]
name = "technical-event-coordinator"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "requests",
    "openai",
    "openenv>=0.2.8"
]

[project.scripts]
server = "server.app:main"