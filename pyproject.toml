[project]
name = "technical-event-coordinator"
version = "0.1.0"
description = "Technical Event Coordinator Environment"

dependencies = [
    "fastapi",
    "uvicorn",
    "requests",
    "openai",
    "openenv"
]

[project.scripts]
server = "app:app"